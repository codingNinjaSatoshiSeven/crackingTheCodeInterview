describe("House Robber spec", function() {
  xit("handles invalid input (array size 0, or not an array) and return -1", function() {
    
  });

  xit("handles base case, 1 element then return that element, 2 element then return the max of the 2", function() {
    
  });

  it("find match", function() {
    expect(strStr('apple pie', 'pie')).toBe(6);
    expect(strStr('apple pie', 'apps')).toBe(-1);
  });

  
});